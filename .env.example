# Required
PDF_PASSWORD=mysecretpassword

# Seconds between folder scans (default: 60)
SCAN_INTERVAL=60

# Path to folder with PDFs (default: ./watch)
WATCH_FOLDER=./watch

# Optional: run container process as specific user/group
# PUID=1000
# PGID=1000
